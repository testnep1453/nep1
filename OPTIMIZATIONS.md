# OPTIMIZATIONS.md — NEP Gaming

| Title | Category | Severity | Impact | Evidence | Recommended Fix | ROI |
|---|---|---|---|---|---|---|
| StudentLogin: Her tuşta localStorage okuması | Unnecessary I/O | High | Her karakter girişinde `getStudents()` localStorage parse ediyor. 4 haneli ID = 4 kez okuma. | `StudentLogin.tsx:25` — `useEffect` `[studentId]` her değişimde `getStudents()` çalışıyor. | `getStudents()` sonucunu `useRef` ile bir kez oku, component mount'ta cache'le. | Yüksek — 4 I/O → 1 I/O |
| StudentLogin: triggerLogin closure stale state | React Re-render | Medium | `triggerLogin` her render'da yeniden tanımlanıyor, closure eski state yakalayabilir. | `StudentLogin.tsx:11-20` — `setLoading`, `setError` bağımlılık yok | `useCallback` ile stabil referans yap veya doğrudan inline kullan. | Orta — tutarlılık |
| App.tsx: student değişiminde gereksiz FCM request | React Re-render | Medium | `requestNotificationPermission()` her `student` değişiminde çalışıyor (logout→login döngü). | `App.tsx:23` — useEffect içinde `student` bağımlılık | Bir kez çağrılacak şekilde `useRef` ile flag'le. | Düşük — nadir trigger |
| App.tsx: lesson tek seferlik yüklenmeli | Firebase I/O | Medium | `getDoc(doc(db, 'lessons', 'nextLesson'))` her student değişiminde yeniden çağrılıyor. | `App.tsx:28-53` — useEffect içinde | `useRef` + flag ile bir kez yükle, veya `lessons` collection'unu ayrı dinle. | Orta — gereksiz Firestore okuma |
| usePresence: Object.keys() ile count hesaplama | Firebase I/O | Medium | `Object.keys(snapshot.val()).length` her data değişiminde tüm objeyi iterate ediyor. | `usePresence.ts:23` — `Object.keys(snapshot.val()).length` | `snapshot.numChildren()` kullan — native, O(1). | Orta — RTDB optimize |
| CircularCountdown: transition-all her saniye 4 SVG | Render Performance | Medium | `transition-all duration-1000` stroke-dashoffset hariç tüm CSS property'leri animasyonlu. 4 circle × 1000ms = gereksiz reflow. | `CircularCountdown.tsx:79` — `className="transition-all"` | `transition-[stroke-dashoffset]` ile sadece gerekli property. + `React.memo` ile circle'ları sar. | Orta — GPU vs CPU animation |
| CircularCountdown: filter: drop-shadow her saniye | GPU/Render | Low-Medium | `filter: drop-shadow(0 0 8px ...)` her frame'de composite/paint. 4 element × 1fps = gereksiz. | `CircularCountdown.tsx:80-82` — inline style filter | Filtreyi statik CSS'e taşı, `will-change: filter` kullan. | Düşük — zaten küçük domain |
| joinClassButton: btoa/unescape UTF-8 | Config | Low | `btoa(unescape(encodeURIComponent(name)))` doğru ama karmaşık. | `JoinClassButton.tsx:29` | `encodeURIComponent(name)` yeter — Zoom bu formatı destekler. | Düşük |
| LoginTransitionOverlay: drop-shadow + animasyon | GPU/Render | Low (1.5s) | `drop-shadow-[0_0_30px_rgba(...)]` paint cost yüksek ama sadece 1.5 saniye çalışıyor. | `LoginTransitionOverlay.tsx:27` | Kabul edilebilir. Not amaçlı. | N/A |
| firebase-messaging-sw.js: placeholder API key | Config | High | `YOUR_API_KEY` gibi placeholder değerler. Push notification asla çalışmaz. | `public/firebase-messaging-sw.js:4-10` | Build sonrası Vite env injection veya script ile güncelle. | Yüksek — feature broken |
| Firebase config: demo-project fallback | Config | Critical | `.env` yoksa `demo-project` ile bağlanıyor → tüm istekler `ERR_BLOCKED_BY_CLIENT`. | `firebase.ts:8-14` — fallback değerler | `.env` şart. Fallback'i kaldır veya uyarı göster. | Kritik — tüm Firebase broken |
| MessageFeed: gereksiz re-render | React Re-render | Low | `fetchedMessages` her Firestore değişikliğinde yeni array → her render. | `MessageFeed.tsx:8-9` — `setMessages(fetchedMessages)` | `useMemo` ile referans stabil tut veya derin karşılaştırma. | Düşük |
